# Take training data, build & return model
build_model <- function(train) {
    return(NULL)
}

# Take created model, run against testing data, & return predictions
build_predictions <- function(model, test) {
    return(NULL)
}

# Create your own functions for evaluating predictions